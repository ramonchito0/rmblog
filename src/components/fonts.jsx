import { Lato, Oswald } from "next/font/google";

export const oswald = Oswald({
  weight: ["400", "700"],
  subsets: ["latin"],
  display: "swap",
});

export const lato = Lato({
  subsets: ["latin"],
  weight: ["400", "700"],
  display: "swap",
});
